# DeerFlow 本地使用指南

## 环境要求

| 工具 | 本机状态 |
|------|---------|
| Node.js (>=22) | ✅ v23.8.0 |
| pnpm | ✅ 11.9.0（通过 Corepack） |
| uv | ✅ 0.10.9 |
| nginx | ✅ 1.21.0 |
| Docker | ❌ 未安装（走本地开发路径） |

## 快速开始

```bash
cd /Users/bilyho/AI/my-deer-flow
```

### 1. 生成配置文件（已完成）

```bash
make config
```

生成 `config.yaml`，之后按需修改。

### 2. 安装依赖（已完成）

```bash
make install
```

默认会自动 `uv sync`（后端）和 `pnpm install`（前端）。如果 pnpm 找不到：

```bash
corepack enable pnpm
```

### 3. 配置模型

#### 方式 A：交互式脚本（推荐）

```bash
make add-model
```

引导式六步走，支持：
- ↑↓ 方向键导航，Enter 确认
- Provider 带图标和描述，一目了然
- api_key 可选择**新建全局变量**、**复用已有 .env 变量**、直接填写、或跳过
- base_url 可沿用 Provider 默认值、自定义、或保存为全局变量
- 预览确认后自动追加到 `config.yaml`，不会丢失原有注释

> **关于「全局变量」**：新建全局变量时，除写入项目 `.env`（DeerFlow 运行时加载）外，还会写入 `~/.deerflow_env`（`export KEY=...`，权限 600），并在 `~/.zshrc` 追加一行 `source ~/.deerflow_env`。写入后需执行 `source ~/.zshrc`（或新开终端）才能在当前 shell 生效。

支持的 Provider：OpenAI 兼容、DeepSeek、阿里云 Coding Plan (Qwen)、火山引擎 (Doubao)、Ollama、Anthropic Claude、Google Gemini、自定义。

#### 方式 B：手动编辑 config.yaml

下面以 Qwen Coding Plan 为例：

```yaml
models:
  - name: qwen3.7-plus
    display_name: Qwen3 3.7-Plus
    use: langchain_openai:ChatOpenAI
    model: qwen3.7-plus
    base_url: https://coding.dashscope.aliyuncs.com/v1
    api_key: $DASHSCOPE_API_KEY
```

然后在 `.env` 中设置：

```
DASHSCOPE_API_KEY=你的阿里云Coding-Plan-API-KEY
```

### .env 注意事项

- DeerFlow **不从 shell 继承环境变量**，`.env` 是唯一来源
- 每个 KEY 必须写**真实值**，不支持 `KEY=$OTHER_VAR` 跨变量引用
- `config.yaml` 中用 `$VAR` 引用 `.env` 中的变量（仅限此方向）
- 某些模型（如 DeepSeek thinking）需用 `PatchedChatDeepSeek` 而非 `ChatOpenAI`，启动时就会校验密钥，密钥有问题会直接报错

### 4. 启动开发服务器

```bash
make dev
```

访问地址：**http://localhost:2026**

首次访问会跳转到 `/setup` 页面，自行填写邮箱和密码创建管理员账号。

## 常用命令

| 命令 | 说明 |
|------|------|
| `make dev` | 开发模式启动（热重载） |
| `make dev-daemon` | 开发模式后台运行 |
| `make start` | 生产模式启动 |
| `make stop` | 停止所有服务 |
| `make clean` | 清理临时文件 |
| `make add-model` | 交互式添加 LLM 模型配置 |
| `make doctor` | 检查配置和依赖健康状态 |
| `make config-upgrade` | 合并新版本配置字段到 config.yaml |
| `make setup-sandbox` | 预拉取沙箱容器镜像（可选） |

## 已知问题与修复

### `make start` 报 `pnpm: command not found`

**表现**：

```
./scripts/serve.sh: line 389: pnpm: command not found
✗ Frontend dependency install failed
```

或启动前端时：

```
env: pnpm: No such file or directory
✗ Frontend failed to start on port 3000 after 120s
```

**原因**：

pnpm 通过 Corepack 安装（`corepack enable pnpm`），在交互式 zsh 终端中正常可用，因为 nvm + corepack 的环境初始化脚本会创建 pnpm shim。但在非交互式 Bash 脚本环境（`serve.sh`）中：

- `check.py`（Python）有 fallback 逻辑——先找 `pnpm`，找不到就用 `corepack pnpm`——所以 `make check` 能通过
- `serve.sh`（Bash）原来直接硬编码 `pnpm`，没有 fallback，所以 `make start`/`make dev` 在依赖安装和启动前端时都会失败

**修复**（已应用到 `scripts/serve.sh`）：

在 `serve.sh` 中新增 `_pick_pnpm()` 函数，与 `check.py` 策略一致——优先使用 `pnpm` 二进制，不可用时 fallback 到 `corepack pnpm`。脚本中所有 3 处 `pnpm` 调用（依赖安装第 404 行、开发模式启动第 313 行、生产模式启动第 319 行）均已替换为通过该函数解析的动态命令。

**如果以后遇到类似问题**：确保 corepack 在 PATH 中（`corepack enable`），或直接安装 pnpm 二进制（`npm install -g pnpm`）。

---

## 项目结构

```
my-deer-flow/
├── backend/          # Python 后端（FastAPI + LangGraph）
│   ├── app/          # 应用代码
│   └── pyproject.toml
├── frontend/         # Next.js 前端
│   └── package.json
├── config.yaml       # 配置文件
├── Makefile          # 统一命令入口
├── docker/           # Docker 相关
├── scripts/          # 脚本工具
│   └── add-model.py  # 交互式模型配置向导
└── 我的使用指南.md
```

## Docker 相关

### Docker 镜像源配置

**问题**：`make docker-start` 时拉取基础镜像超时，无法连接 Docker Hub：
```
dial tcp 103.200.31.172:443: i/o timeout
```

**原因**：国内网络无法直达 Docker Hub，需要配置镜像加速器。

**解决方案**：编辑 `~/.docker/daemon.json`，添加可用的镜像源：

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live",
    "https://hub.rat.dev",
    "https://docker.1ms.run"
  ]
}
```

**重启 Docker Desktop**：
```bash
# 命令行重启
osascript -e 'quit app "Docker"' && sleep 3 && open -a Docker

# 或手动操作：菜单栏 Docker 图标 → Quit Docker Desktop，然后重新打开
```

等待 Docker Desktop 完全启动后，再次运行 `make docker-start`。

**验证镜像源是否生效**：
```bash
docker info | grep -A 5 "Registry Mirrors"
```

**手动测试拉取镜像**：
```bash
docker pull python:3.12-slim-bookworm
docker pull node:22-alpine
```

### Docker 启动命令

| 命令 | 说明 |
|------|------|
| `make docker-start` | Docker 开发模式启动 |
| `make docker-stop` | 停止 Docker 容器 |
| `make docker-logs` | 查看容器日志 |
| `make up` | Docker 生产模式构建并启动 |
| `make down` | 停止并移除 Docker 容器 |

## 其他模型配置参考

<details>
<summary>DeepSeek</summary>

```yaml
- name: deepseek-v3
  display_name: DeepSeek V3
  use: langchain_openai:ChatOpenAI
  model: deepseek-chat
  api_key: $DEEPSEEK_API_KEY
  base_url: https://api.deepseek.com/v1
  request_timeout: 600.0
  max_retries: 2
  max_tokens: 8192
```

</details>

<details>
<summary>OpenAI</summary>

```yaml
- name: gpt-4
  display_name: GPT-4
  use: langchain_openai:ChatOpenAI
  model: gpt-4
  api_key: $OPENAI_API_KEY
  request_timeout: 600.0
  max_retries: 2
  max_tokens: 4096
```

</details>

<details>
<summary>本地 Ollama</summary>

```yaml
- name: qwen3-local
  display_name: Qwen3 32B (Ollama)
  use: langchain_ollama:ChatOllama
  model: qwen3:32b
  base_url: http://localhost:11434
  num_predict: 8192
  temperature: 0.7
```

</details>
